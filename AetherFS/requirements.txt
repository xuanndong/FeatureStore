# ==========================================================
# AETHER - SERVICE REQUIREMENTS (PHOTON & TRANSFORMATION)
# ==========================================================

# --- Core API Framework ---
fastapi[standard]
python-dotenv
python-multipart
virtualenv
psutil

# --- Distributed Computing (Ray Client) ---
# Đồng bộ hoàn toàn phiên bản với hạ tầng cụm Ray Cluster
ray[default]==2.55.1
pandas

# --- Database & ORM (Metadata & Tracking) ---
sqlmodel
alembic
asyncpg
psycopg2-binary

# --- Messaging & Cache ---
confluent-kafka
redis

# --- Storage Connectors & Query Engine ---
fsspec
s3fs
duckdb
pyarrow

# --- Protocol ---
grpcio
grpcio-tools
grpcio-reflection

# --- Webhook Callback ---
httpx
